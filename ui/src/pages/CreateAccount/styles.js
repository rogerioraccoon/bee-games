import styled from "styled-components";
import { Content, Input, Button } from "../../styles/common";

export const ContentPage = styled(Content)`
    text-align: center;
`

export const InputPage = styled(Input)`
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
`;

export const ButtonPage = styled(Button)`
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
`;
